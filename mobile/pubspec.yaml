name: synapse_trade
description: SynapseTrade - La capa de inteligencia para la navegación de mercados de élite
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  provider: ^6.1.1
  http: ^1.2.0
  shimmer: ^3.0.0
  fl_chart: ^0.66.2
  intl: ^0.19.0
  flutter_animate: ^4.3.0
  web_socket_channel: ^2.4.0
  webview_flutter: ^4.7.0
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  google_sign_in: ^7.2.0
  cloud_firestore: ^5.4.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

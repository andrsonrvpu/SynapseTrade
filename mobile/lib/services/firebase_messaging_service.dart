/// Push notification service stub.
/// Firebase messaging is disabled for the web build.
/// Re-enable by adding firebase_messaging to pubspec.yaml and running flutterfire configure.
class PushNotificationService {
  void init(dynamic provider) {}
  Future<void> setupInteractedMessage() async {}
}
